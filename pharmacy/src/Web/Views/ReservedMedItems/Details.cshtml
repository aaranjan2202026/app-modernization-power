@model PharmacyNetwork.ApplicationCore.Entities.ReservedMedItem

@{
    ViewData["Title"] = "Reserve Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="mb-3 text-center">Reserve Details</h1>

<div>
    <h4 class="text-center mb-3">Phone: @Html.DisplayFor(model => model.Telephone)</h4>
    <h5 class="text-center mb-3">@Html.DisplayFor(model => model.PharmacyWharehouse.Pharm.PharmName)</h5>
    <hr />
    <h5 class="text-center mb-4">@Html.DisplayFor(model => model.PharmacyWharehouse.MedItem.MedItemName) - @Html.DisplayFor(model => model.Count) pcs.</h5>
    <hr />

    <dl class="row">
        <dt class="col-sm-4">
            Reservation Start
        </dt>
        <dd class="col-sm-8">
            @Html.DisplayFor(model => model.DateStart)
        </dd>
        <dt class="col-sm-4">
            Reservation End
        </dt>
        <dd class="col-sm-8">
            @Html.DisplayFor(model => model.DateFinish)
        </dd>
    </dl>
</div>

<div>
    <a asp-action="Index" class="btn btn-secondary btn-lg">Back</a>| 
    <a asp-action="Remove" asp-route-id="@Model.ReservedId" class="btn btn-danger btn-lg">Remove Reserve</a>
</div>
