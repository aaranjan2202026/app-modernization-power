@model PharmacyNetwork.Web.ViewModels.MedicalItemsListViewModel

<form asp-action="Index">
    <div class="form-group row">
        <label class="col-form-label col-lg-2">Category</label>
        <div class="col-lg-5">
            <select asp-for="@Model.CategoryFilterApplied" class="form-control" asp-items="@Model.Categories"></select>
        </div>
    </div>
    <div class="form-group row">
        <label class="col-form-label col-lg-2">Firm</label>
        <div class="col-lg-5">
            <select asp-for="@Model.FirmFilterApplied" class="form-control" asp-items="@Model.Firms"></select>
        </div>
    </div>

    <input type="submit" value="Apply" class="btn btn-secondary mb-3"/>
</form>